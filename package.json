{
  "name": "applicot",
  "version": "0.2.0",
  "type": "module",
  "description": "Multi-agent novel writing system (MCP-based)",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "mcp-server": "bun run src/mcp/server.ts",
    "scaffold": "bun run src/cli/scaffold.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
